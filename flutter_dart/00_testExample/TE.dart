void main() {
  int x = 10;
  int y = 5;

  // Assert that y is less than x
  assert(y < x);

  // This code will execute if the assertion passes
  print('Assertion passed!');

  // If the assertion fails (y is not less than x), an assertion error will occur
  // and the program will terminate
}
