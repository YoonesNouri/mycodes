class Quote {
  String text;
  String author;

  Quote({required this.text, required this.author}); // constructor
}

// Quote myquote = Quote(text: 'this is quote text,', author: ' jams bond'); //instantiation نمونه سازی

// output: this is quote text, jams bond