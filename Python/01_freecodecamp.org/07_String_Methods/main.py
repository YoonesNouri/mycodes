capitalize(): رشته را به صورتی با حرف اول بزرگ و بقیه حروف کوچک تبدیل می‌کند.
upper(): تمام حروف رشته را به حروف بزرگ تبدیل می‌کند.
lower(): تمام حروف رشته را به حروف کوچک تبدیل می‌کند.
title(): هر کلمه از رشته را با حرف اول آن بزرگ و سایر حروف آن کوچک می‌کند.
strip(): فاصله‌های اضافی در ابتدا و انتهای رشته را حذف می‌کند.
replace(old, new): تمام زیررشته‌هایی که با old مطابقت دارند را با new جایگزین می‌کند.
split(delimiter): رشته را بر اساس delimiter به یک لیست از زیررشته‌ها تقسیم می‌کند.
join(iterable): رشته را با اتصال زیررشته‌های موجود در iterable ایجاد می‌کند.
startswith(prefix): مشخص می‌کند که آیا رشته با prefix شروع می‌شود یا خیر.
endswith(suffix): مشخص می‌کند که آیا رشته با suffix پایان می‌یابد یا خیر.
همچنین می‌توانید از متد‌های دیگری مانند find()، count()، index() و ... نیز استفاده کنید.