//9.1

(function () {
    'use strict';
    // Get the parent element
    const parentElement = document.querySelector('.parent');

    // Select the first child element using :first-child CSS selector
    const firstChildElement = parentElement.querySelector(':first-child');

    // Apply styles or perform other operations on the first child element
    firstChildElement.style.fontWeight = 'bold';
    firstChildElement.style.color = 'blue';
}());

(function () {
    'use strict';

}());


