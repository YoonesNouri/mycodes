// myModule.js

// Exporting individual items
export const variable = 42;
export function greet(name) {
    return `Hello, ${name}!`;
}

// Exporting with a default item
const defaultItem = 'This is the default item';
export default defaultItem;
