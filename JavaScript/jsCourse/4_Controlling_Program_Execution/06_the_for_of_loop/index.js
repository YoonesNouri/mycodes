//4.6

(function () {
    'use strict';

    // for...of loop: Iterates over iterable objects such as arrays, strings, maps, sets and collections.
    const arr = [1, 2, 3];
    for (let value of arr) {
        console.log(value);
    }
    //output:
    // 1
    // 2
    // 3


}());